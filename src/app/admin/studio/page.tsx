import { StudioEntry } from './_components/studio-entry';

export const metadata = { title: 'Studio | Endorfin', robots: { index: false, follow: false } };

export default function StudioEntryPage() {
  return <StudioEntry />;
}
