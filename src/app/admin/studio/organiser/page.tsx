import { OrganiserShell } from './_components/organiser-shell';

export const metadata = {
  title: 'Organiser home | Endorfin Studio',
  robots: { index: false, follow: false },
};

export default function OrganiserHomePage() {
  return <OrganiserShell />;
}
