import { MyClubsContent } from '../_components/my-clubs';

export const metadata = { title: 'Your clubs | Endorfin', robots: { index: false, follow: false } };

export default function StudioMyClubsPage() {
  return <MyClubsContent />;
}
